- test with some real data
- test + optimize index view (select_related needed probably)
- something better with the map display:
    - slippy map plus points?
    - green/red line showing progress?

MAYBE:
- fetch from inaturalist api?
- breadcrumbs: big map display?
